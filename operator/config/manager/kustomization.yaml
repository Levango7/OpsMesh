apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - manager.yaml
# `make deploy` runs `kustomize edit set image controller=${IMG}` against this
# kustomization, so we declare the image replacement target here.
images:
  - name: controller
    newName: opsmesh/operator
    newTag: latest